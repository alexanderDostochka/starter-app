import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  description: {
    padding: 20,
    marginBottom: 20,
    textAlign: 'center',
  },
  emptyMessage: {
    marginTop: 15,
    textAlign: 'center',
  },
});
